// @ts-check
// / <reference types="webpack" name="webpack" />
'use strict';

/**
 * The compilation dirty checker tries to find out if any
 * dependency of the html-webpack-plugin template rendering process
 * has changed (in most cases this is just the template file)
 *
 * @property {number} cacheTimestamp
 */
class CompilationDirtyChecker {

  /**
   * @param {any} compilation
   */
  markCompilationAsLatestCachedCompilation (compilation) {
    this.cacheTimeStamp = Date.now();
  }

  isCompilationCached () {

  }
}

module.exports = CompilationDirtyChecker;
